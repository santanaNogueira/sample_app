<header class="navbar navbar-fixed_top">
		<div class="navbar-inner">
			<div class="container">
				<%= link_to "twitter", root_path, id:"logo"%> <!-- id:"logo" == :id => "logo" -->
				<nav>
					 <ul class="nav pull-right">
					 	<li><%= link_to "Home", 	root_path%></li>
					 	<li><%= link_to "Help", 	help_path%></li>
					 	<% if signed_in? %>
					 		<li><%= link_to "Users", 	clients_path%></li>
					 		<li id="fat-menu" class="dropdown">
					 			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
					 				Account <b class="caret"></b>
					 			</a>
					 			<ul class="dropdown-menu">
					 				<li><%= link_to "Profile", client_path(current_client) %></li>
					 				<li><%= link_to "Settings", edit_client_path(current_client) %></li>
					 				<li class="divider"></li>
					 				<li>
					 					<%= link_to "Sign out", signout_path, method: "delete" %>
					 				</li>
					 			</ul>
					 		</li>
					 	<% else %>
					 		<li><%= link_to "Sign in", 	signin_path%></li>
					 	<% end %>
					 </ul>
				</nav>
			</div>
		</div>
	</header>