<%provide(:title, 'Sign up')%>
<h1>Regista-te</h1>


<dic class="row">
	<dic class="span6 offset3">

		<%= form_for(@client) do |f| %>
			<%= render 'shared/error_messages' %>
			<%= f.label :name, "Nome" %>
			<%= f.text_field :name  %>

			<%= f.label :email %>
			<%= f.text_field :email %>

			<%= f.label :password %>
			<%= f.password_field :password %>

			<%= f.label :password_confirmation, "Confirmação" %>
			<%= f.password_field :password_confirmation %>

			<%= f.submit "Cria aqui a tua conta!", class: "btn btn-large btn-inverse"%>
			
		<% end %>

	</div>
</div>