<%= provide(:title, @client.name) %>

<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @client, size: 100 %>
				<%= @client.name %>
			</h1>
		</section>
	</aside>
</div>